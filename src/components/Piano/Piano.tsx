import { useEffect } from "react";
import { keys, playNote, handlePressKey } from "../../utils/tone";

export default function Piano() {
    return (
        <div className="bg-white w-full lg:h-[22rem] flex justify-center items-center">
            Piano
        </div>
    );
}